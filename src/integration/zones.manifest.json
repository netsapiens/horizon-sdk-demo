{
  "$comment": "Canonical map of every surface this demo contributes to a Horizon host: SDK zone extensions, full-page routes, dynamic columns, and dashboard widgets. Single source of truth shared by the app (src/integration/zones.ts drives registration from it) and the netsapiens-horizon-testing Playwright suite (asserts each testId mounts). testId scheme: extensions 'sdk-demo-ext-<id minus demo- prefix>', routes 'sdk-demo-page-<slug>', columns 'sdk-demo-col-<field>', dashboard widgets 'sdk-demo-widget-<widgetId>'. Each injected extension is also tagged with data-zone. The testId + data-zone ride on each contributed component's OWN root element (no wrapper element), so the marker is a real, visible box — assert VISIBILITY (toBeVisible). A component that renders null (an inactive zone) has no marker, which is the accurate signal. 'version' mirrors package.json version and is the value to register the app under: the remote entry URL is stable, so the registered version is the only thing that tells Horizon to re-verify the bytes at that URL. It must match package.json — the deploy workflow fails the build if it drifts.",
  "appId": "horizon-extension-demo",
  "webpackModule": "horizonExtensionDemo",
  "remoteEntryUrl": "https://netsapiens.github.io/horizon-sdk-demo/remoteEntry.js",
  "$comment_requiredScopes": "Where a route, column or widget declares `requiredScopes`, it is recorded here so the Playwright suite knows which signed-in scope can assert that surface — but the manifest is NOT what registers it. The enforcing value is an inline string literal at the registerRoute/registerDynamicColumn/registerWidget call site in App.tsx, because the platform analyser extracts an app's declared surface by reading string literals out of submitted source and cannot follow a constant or a loop. The duplication is deliberate; keep the two in step.",
  "$comment_widgets": "Dashboard widgets are NOT auto-mounted, and that is the contract working, not a bug: registering a widget makes it eligible, and a saved layout is authoritative, so a newly available widget appears in a dashboard's Customize catalogue rather than placing itself on somebody's grid. The Playwright suite must therefore ADD the widget from Customize before it can assert its testId. `zones` is the whole list the widget is eligible on; there is no data-zone attribute on a widget, because the same component can mount on any of them and a single zone attribute would name the wrong one. Like `requiredScopes`, `zones` here is documentation — the enforcing declaration is the inline literal array at the registerWidget call site in App.tsx, for the same string-literal-extraction reason.",
  "version": "1.8.7",
  "extensions": [
    {
      "id": "demo-export-button",
      "zone": "page-header-actions",
      "routes": [
        "/manage/call-logs",
        "/manage/*/call-logs",
        "/manage/*/contacts",
        "/manage/*/devices/registrations",
        "/manage/*/users"
      ],
      "priority": 10,
      "testId": "sdk-demo-ext-export-button"
    },
    {
      "id": "demo-analytics-widget",
      "zone": "page-content-after",
      "routes": ["/manage/call-logs", "/manage/:domain/call-logs"],
      "priority": 100,
      "requiredPermissions": ["call-events:subscribe"],
      "testId": "sdk-demo-ext-analytics-widget"
    },
    {
      "id": "demo-quick-action",
      "zone": "table-row-actions",
      "routes": ["/manage/call-logs", "/manage/*/call-logs"],
      "testId": "sdk-demo-ext-quick-action"
    },
    {
      "id": "demo-caller-info-widget",
      "zone": "inbound-call-content",
      "routes": ["/*"],
      "priority": 100,
      "requiredPermissions": ["call-events:listen"],
      "testId": "sdk-demo-ext-caller-info-widget"
    },
    {
      "id": "demo-contact-form-banner",
      "zone": "form-section-before",
      "routes": ["/manage/*/contacts", "/home/contacts"],
      "priority": 100,
      "testId": "sdk-demo-ext-contact-form-banner"
    },
    {
      "id": "demo-compliance-checkbox",
      "zone": "form-section-after",
      "routes": ["/manage/*/contacts", "/home/contacts"],
      "priority": 50,
      "testId": "sdk-demo-ext-compliance-checkbox"
    },
    {
      "id": "demo-active-calls-recording-filter",
      "zone": "table-filter-bar",
      "routes": ["/manage/active-calls", "/manage/*/active-calls"],
      "priority": 10,
      "testId": "sdk-demo-ext-active-calls-recording-filter"
    },
    {
      "id": "demo-header-status-badge",
      "zone": "page-header-secondary",
      "routes": ["/*"],
      "testId": "sdk-demo-ext-header-status-badge"
    },
    {
      "id": "demo-table-toolbar-info",
      "zone": "table-toolbar",
      "routes": ["/manage/call-logs", "/manage/*/call-logs"],
      "testId": "sdk-demo-ext-table-toolbar-info"
    },
    {
      "id": "demo-topbar-help",
      "zone": "topbar-actions",
      "routes": ["/*"],
      "testId": "sdk-demo-ext-topbar-help"
    }
  ],
  "routes": [
    {
      "id": "ucaas-demo-page",
      "parentPath": "/apps",
      "path": "horizon-sdk-demo",
      "fullPath": "/apps/horizon-sdk-demo",
      "testId": "sdk-demo-page-demo"
    },
    {
      "id": "ucaas-component-showcase",
      "parentPath": "/apps",
      "path": "component-showcase",
      "fullPath": "/apps/component-showcase",
      "requiredScopes": "DOMAIN_MANAGERS",
      "testId": "sdk-demo-page-component-showcase"
    },
    {
      "id": "ucaas-crm-integration",
      "parentPath": "/manage",
      "path": "crm-integration",
      "fullPath": "/manage/crm-integration",
      "testId": "sdk-demo-page-crm-integration"
    },
    {
      "$comment": "Domain-scoped: ':domain' is a token the host fills from the URL, so this single registration serves every domain the admin drills into. 'fullPath' is therefore a pattern — substitute the selected domain to get a concrete URL.",
      "id": "ucaas-domain-crm-sync",
      "parentPath": "/manage/:domain",
      "path": "crm-sync",
      "fullPath": "/manage/:domain/crm-sync",
      "requiredScopes": "DOMAIN_MANAGERS",
      "testId": "sdk-demo-page-domain-crm-sync"
    },
    {
      "$comment": "Built from templates.DashboardTemplate — the host's dashboard shell and card frames on a page the app owns, with no Customize or Reorder. Contrast the widgets array below, which registers cards into the host's OWN dashboards.",
      "id": "ucaas-vendor-dashboard",
      "parentPath": "/apps",
      "path": "example-crm",
      "fullPath": "/apps/example-crm",
      "testId": "sdk-demo-page-vendor-dashboard"
    },
    {
      "id": "ucaas-call-recordings",
      "parentPath": "/home",
      "path": "call-recordings",
      "fullPath": "/home/call-recordings",
      "testId": "sdk-demo-page-call-recordings"
    }
  ],
  "columns": [
    {
      "id": "demo-call-priority-column",
      "zone": "call-logs-columns",
      "field": "call-priority",
      "routes": ["/manage/call-logs", "/manage/*/call-logs"],
      "requiredScopes": "PLATFORM",
      "testId": "sdk-demo-col-call-priority"
    }
  ],
  "widgets": [
    {
      "id": "demo-recent-activity",
      "widgetId": "recent-activity",
      "storedId": "horizon-extension-demo:recent-activity",
      "kind": "panel",
      "zones": ["platform-admin-dashboard-widgets", "manage-dashboard-widgets"],
      "category": "activity",
      "size": "half",
      "placement": { "after": "health" },
      "refreshPolicy": "shared-range",
      "requiredScopes": "ADMINS",
      "testId": "sdk-demo-widget-recent-activity"
    },
    {
      "$comment": "Declares `metric`, which the host frame draws as a provenance tooltip beside the title. Left at the default chrome: every widget here lets the frame draw the title, the description as a subtitle, and the shared-range window chip.",
      "id": "demo-call-volume",
      "widgetId": "call-volume",
      "storedId": "horizon-extension-demo:call-volume",
      "kind": "panel",
      "zones": ["platform-admin-dashboard-widgets"],
      "category": "charts",
      "size": "full",
      "height": 5,
      "resizable": true,
      "placement": { "after": "average-calls" },
      "refreshPolicy": "shared-range",
      "testId": "sdk-demo-widget-call-volume"
    },
    {
      "$comment": "own-cadence: the widget owns a timer and the host hands it no range. Its Refresh remounts the component, which resets the poll counter it prints.",
      "id": "demo-sync-queue",
      "widgetId": "sync-queue",
      "storedId": "horizon-extension-demo:sync-queue",
      "kind": "panel",
      "zones": ["platform-admin-dashboard-widgets"],
      "category": "tables",
      "size": "half",
      "height": 4,
      "resizable": true,
      "placement": { "after": "suspect-domains" },
      "refreshPolicy": "own-cadence",
      "testId": "sdk-demo-widget-sync-queue"
    },
    {
      "$comment": "realtime: data arrives by push on the app-scoped event bus. Gated on the app's own call-events grant, so the suite must sign in as a user of an app that holds it AND drive a call before this card has anything to assert beyond its zero state.",
      "id": "demo-live-calls",
      "widgetId": "live-calls",
      "storedId": "horizon-extension-demo:live-calls",
      "kind": "panel",
      "zones": ["platform-admin-dashboard-widgets"],
      "category": "activity",
      "size": "half",
      "height": 4,
      "resizable": true,
      "placement": { "after": "health" },
      "refreshPolicy": "realtime",
      "requiredPermissions": ["call-events:listen"],
      "testId": "sdk-demo-widget-live-calls"
    },
    {
      "$comment": "A CONTAINER the app ships itself: its body is drawn by the host's LeafContainer, not by the registered component, and its leaves are the two demo-insight entries below. The category is namespaced ('demo-insight', not 'stat') so it cannot collide with the host's own stat card.",
      "id": "demo-integration-health",
      "widgetId": "integration-health",
      "storedId": "horizon-extension-demo:integration-health",
      "kind": "panel",
      "zones": ["platform-admin-dashboard-widgets"],
      "category": "other",
      "acceptsLeaves": "demo-insight",
      "size": "half",
      "height": 3,
      "resizable": true,
      "placement": "end",
      "testId": "sdk-demo-widget-integration-health"
    },
    {
      "id": "demo-contacts-synced",
      "widgetId": "contacts-synced",
      "storedId": "horizon-extension-demo:contacts-synced",
      "kind": "leaf",
      "leafOf": "demo-insight",
      "zones": ["platform-admin-dashboard-widgets"],
      "category": "stats",
      "testId": "sdk-demo-widget-contacts-synced"
    },
    {
      "id": "demo-sync-failures",
      "widgetId": "sync-failures",
      "storedId": "horizon-extension-demo:sync-failures",
      "kind": "leaf",
      "leafOf": "demo-insight",
      "zones": ["platform-admin-dashboard-widgets"],
      "category": "stats",
      "testId": "sdk-demo-widget-sync-failures"
    },
    {
      "$comment": "No refreshPolicy at all: nothing here goes stale, so declaring one would invite a dependency on the dashboard's range control that this card does not have.",
      "id": "demo-partner-links",
      "widgetId": "partner-links",
      "storedId": "horizon-extension-demo:partner-links",
      "kind": "panel",
      "zones": ["platform-admin-dashboard-widgets"],
      "category": "other",
      "size": "half",
      "height": 3,
      "resizable": true,
      "placement": "end",
      "testId": "sdk-demo-widget-partner-links"
    },
    {
      "id": "demo-recorded-calls",
      "widgetId": "recorded-calls",
      "storedId": "horizon-extension-demo:recorded-calls",
      "kind": "leaf",
      "leafOf": "stat",
      "zones": ["platform-admin-dashboard-widgets"],
      "category": "stats",
      "testId": "sdk-demo-widget-recorded-calls"
    }
  ]
}
